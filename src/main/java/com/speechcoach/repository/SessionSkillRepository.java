package com.speechcoach.repository;

import com.speechcoach.domain.SessionSkill;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SessionSkillRepository extends JpaRepository<SessionSkill, Long> {
}