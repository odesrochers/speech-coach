spring:
  application:
    name: speechcoach
  datasource:
    url: ${DATABASE_URL}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

anthropic:
  api-key: ${ANTHROPIC_API_KEY}
